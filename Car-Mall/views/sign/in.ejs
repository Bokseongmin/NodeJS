<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <script src="http://code.jquery.com/jquery.js"></script>
    <title>Document</title>
</head>

<body>
    <div class="container">
        <h1 class="row justify-content-center mt-5">Comstudy 자동차</h1>
        <div class="row justify-content-center mt-3">
            <div class="col-4">
                <% if (name) { %>
                    <p>Welcome <%= name %>!</p>
                    <button class="btn btn-primary" id="logout">로그아웃</button>

                    <% if (lv==0) { %>
                        <button class="btn btn-primary" id="adminBtn">관리</button>
                    <% } %>
                    
                    <% } else { %>
                        <form action="/sign/in" method="post">
                            <div class="mb-3">
                                <label class="form-label">아이디</label>
                                <input class="form-control" type="text" name="userId" id="userId" value="hong" />
                            </div>
                            <div class="mb-3">
                                <label for="passwd" class="form-label">패스워드</label>
                                <input class="form-control" type="password" name="passwd" id="passwd" value="1234" />
                            </div>
                            <button class="btn btn-primary" type="submit" style="width: 100%;">로그인</button>
                        </form>
                    <% } %>
                    
                    <button class="btn btn-primary mt-2" id="upBtn" style="width: 100%;">회원가입</button>
            </div>
        </div>
    </div>
    <script>
        $("#upBtn").click(function() {
            location.href = "/sign/up";
        });
    </script>
</body>

</html>